# Claude Code

Follow [AGENTS.md](./AGENTS.md) for all repository-specific instructions.

Claude-specific notes:

- Keep `AGENTS.md` as the canonical source; do not duplicate rule text here.
- When editing `devenv/**`, also follow `devenv/AGENTS.md` and `devenv/CLAUDE.md`.
- Do not add `.claude/settings.json`, `.claude/agents`,
  `.claude/commands`, or `.claude/skills` unless a task explicitly needs
  versioned Claude-only workflow automation.
